
import 'package:flutter/material.dart';
class HangmanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Your Hangman page implementation
    return Scaffold(
      appBar: AppBar(
        title: Text('Hangman'),
      ),
      body: Center(
        child: Text('Hangman Page'),
      ),
    );
  }
}
